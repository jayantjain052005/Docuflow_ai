# DocuFlow AI - Python Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────────────────
Flask==3.0.3
Werkzeug==3.0.3              # Password hashing + secure filename

# ── AI / Gemini ────────────────────────────────────────────────────────────────
#google-generativeai==0.7.2     # Official Gemini API client (gemini-1.5.0-flash)
groq==1.2.0
# ── OCR ────────────────────────────────────────────────────────────────────────
pytesseract==0.3.10          # Python wrapper for Tesseract OCR
Pillow==10.4.0               # Image processing (required by pytesseract)

# ── PDF Parsing ────────────────────────────────────────────────────────────────
pdfplumber==0.11.0           # Extract text from PDF files

# ── Environment Variables ──────────────────────────────────────────────────────
python-dotenv==1.0.1         # Load .env file into os.environ
opencv-python==4.10.0.84
twilio
# pip install 
google-api-python-client 
google-auth-httplib2 
google-auth-oauthlib
google-api-python-client 
google-auth-httplib2 
google-auth-oauthlib

flask-sqlalchemy
flask-migrate
flask-bcrypt
flask-jwt-extended
# DocuFlow AI - OCR Pipeline Requirements
# Install with: pip install -r requirements.txt

# Core OCR
pytesseract>=0.3.10
Pillow>=10.0.0

# Computer Vision
opencv-python>=4.8.0

# PDF Processing
pdfplumber>=0.10.0
pypdf>=4.0.0

# Numerical computing
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0

# Optional: for faster image loading
imageio>=2.31.0

# Development / testing
pytest>=7.4.0
requests
